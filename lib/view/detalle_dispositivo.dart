import 'package:flutter/material.dart';

class DetalleDispositivo extends StatelessWidget {
  const DetalleDispositivo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text("Detalle"),
        ),
        body: Column(
          children: const [],
        ));
  }
}
